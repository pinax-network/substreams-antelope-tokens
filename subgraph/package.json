{
  "name": "antetokens",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ antetokens",
    "create-local": "graph create --node http://localhost:8020/ antetokens",
    "remove-local": "graph remove --node http://localhost:8020/ antetokens",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 antetokens",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.73.0"
  },
  "version": "v0.0.1"
}
